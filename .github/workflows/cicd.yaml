name: "CI/CD"

on:
  workflow_run:
    workflows: ["Spring Test"]
    types:
      - completed

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: test
        run: |
          echo "test"

# TODO: ghcr.io & Argo CD (related: #22)
